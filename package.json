{
  "name": "mktraders-full",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "concurrently \"npm:server\" \"npm:client\"",
    "bootstrap": "cd server && npm install && cd ../client && npm install",
    "server": "cd server && node index.js",
    "client": "cd client && npm run dev"
  },
  "devDependencies": {
    "concurrently": "^7.6.0"
  }
}